// #Pre-Processor directives <Header-file>
#include <stdio.h>

// main() function with integer return type.
int main() {
    // printf() is the built-in functions,that show the output.
    printf("hello world!");

    return 0;
}
