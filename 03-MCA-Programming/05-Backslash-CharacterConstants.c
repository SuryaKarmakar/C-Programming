#include <stdio.h>

int main() {
    /*
    '\b' = back space
    '\f' = form feed
    '\n' = new line
    '\r' = carriage return
    '\t' = horizontal tab
    '\v' = vertical tab
    '\'' = single quote
    '\"' = double quote
    '\\' = backslash
    '\0' = null
    */

    return 0;
}