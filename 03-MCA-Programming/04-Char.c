#include <stdio.h>

int main() {
    // Character constants have integer values known as ASCII values.
    printf("ASCII value of 'a' is %d \n", 'a');
    printf("ASCII value of 97 is %c", 97);

    return 0;
}