#include<stdio.h>
// we need to include the math.h header file to using this pow() function.
#include<math.h>

int main(){
	int a;
	// pow(x,y) => x to the power y
	a = pow(3,5);
	printf("3 to the power 5 is : %d \n",a);
	return 0;
}

