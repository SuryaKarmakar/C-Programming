#include<stdio.h>

int main(){
	// int => keyword(also know as 'Reserved' words)
	// var => variable(also know as 'Identifiers' and it can be changed)
	// 10 => constant(also know as 'Literals' and it can't be changed)
	int var = 10;
	return 0;
}