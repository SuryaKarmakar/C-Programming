#include <stdio.h>

int main(){
    /* here those 3 are declared as a pointer variable, that's means they can capable of holding address. The declaration float *f dose not mean
    that 'f' is going to contain a floating-point value. What it means is 'f' is going to contain the address of a floating-point value... */
    int *i;
    char *ch;
    float *f;

    return 0;
}